---
phase: 01-base-confi-vel
plan: 01
type: tracer
wave: 1
depends_on: []
files_modified:
  - src/lib/cnab/validate-line.ts
  - src/lib/cnab/types.ts
  - src/lib/cnab/validate-line.test.ts
autonomous: true
requirements: [BUG-02]
must_haves:
  truths:
    - "Usuário vê warning UNKNOWN_RECORD ou NO_SPEC no relatório quando linha não tem spec (nível B/AB)"
    - "Linha sem spec aparece em lineDetails com label sintético"
    - "Nível A não emite warnings de spec ausente"
  artifacts:
    - path: src/lib/cnab/validate-line.ts
      provides: Emissão de issues para classify/spec miss
    - path: src/lib/cnab/validate-line.test.ts
      provides: Testes UNKNOWN_RECORD e NO_SPEC
  key_links:
    - from: src/lib/cnab/validate-line.ts
      to: src/lib/cnab/types.ts
      via: ValidationIssue codes
---

<objective>
Eliminar silêncio em linhas/registros sem spec: emitir warnings `UNKNOWN_RECORD` / `NO_SPEC` visíveis em issues e lineDetails (nível B/AB apenas).

Purpose: Primeiro slice end-to-end da Fase 1 — operador passa a ver registros não validados em vez de omissão silenciosa.
Output: validate-line.ts atualizado + testes.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-base-confi-vel/01-CONTEXT.md
@.planning/ROADMAP.md
@src/lib/cnab/validate-line.ts
@src/lib/cnab/spec-registry.ts
@src/lib/cnab/fixtures.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emitir warnings para linhas sem spec</name>
  <files>src/lib/cnab/validate-line.ts, src/lib/cnab/types.ts</files>
  <read_first>src/lib/cnab/validate-line.ts, src/lib/cnab/validate-shared.ts</read_first>
  <action>
    Refatorar `validateLine` e `validateFileLines`:
    - Quando `classifyLine()` retorna null: criar `LineValidationResult` sintético com issue `severity: 'warning'`, `code: 'UNKNOWN_RECORD'`, mensagem indicando tipo de registro não classificado.
    - Quando `getRecordSpec()` retorna null: issue `code: 'NO_SPEC'`, mensagem indicando spec ausente para recordType/layout/bankId/kind.
    - Retornar entrada em `lineDetails` (não pular com `continue`).
    - Em `validateRemessa`/`validateRetorno`: só chamar validateFileLines quando `level !== 'A'` (já existe — garantir que warnings só aparecem em B/AB).
    Usar padrão `push()` de validate-shared.ts para consistência se aplicável.
  </action>
  <verify>npm test -- validate-line</verify>
  <acceptance_criteria>
    - validate-line.ts não retorna null silenciosamente em validateFileLines
    - Códigos UNKNOWN_RECORD e NO_SPEC presentes no código
  </acceptance_criteria>
  <done>validateFileLines inclui todas as linhas processadas em level B/AB; warnings emitidos</done>
</task>

<task type="auto">
  <name>Task 2: Testes validate-line para unspec</name>
  <files>src/lib/cnab/validate-line.test.ts, src/lib/cnab/fixtures.ts</files>
  <read_first>src/lib/cnab/validate-line.test.ts, .planning/phases/01-base-confi-vel/01-CONTEXT.md</read_first>
  <action>
    Adicionar testes em português:
    - Caso registro tipo desconhecido em CNAB400 → issue UNKNOWN_RECORD
    - Caso recordType sem spec no registry → issue NO_SPEC
    - Caso level=A → sem warnings de spec (integração via validateRemessa se já existir padrão)
    Usar mutação de fixture existente (remessa400Bradesco) quando possível.
  </action>
  <verify>npm test -- validate-line</verify>
  <acceptance_criteria>
    - Testes passam
    - issues.some(i => i.code === 'UNKNOWN_RECORD' || i.code === 'NO_SPEC') assertado
  </acceptance_criteria>
  <done>Cobertura de BUG-02 com testes dedicados</done>
</task>

</tasks>

<verification>
- [ ] npm test
- [ ] npm run build
</verification>

<success_criteria>
- Warnings visíveis para linhas sem spec em nível B/AB
- Testes validate-line passando
- Sem regressão nos testes existentes
</success_criteria>

<output>
After completion, create `.planning/phases/01-base-confi-vel/01-01-SUMMARY.md`
</output>
