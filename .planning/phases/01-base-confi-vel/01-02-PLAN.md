---
phase: 01-base-confi-vel
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/cnab/parse-240.ts
  - src/lib/cnab/validate.ts
  - src/lib/cnab/validate-retorno.ts
  - src/lib/cnab/fixtures.ts
  - src/lib/cnab/parse-240.test.ts
autonomous: true
requirements: [BUG-03]
must_haves:
  truths:
    - "Arquivo CNAB240 com 2+ lotes popula Parsed240.lotes[] corretamente"
    - "Contadores T240_LOTE_COUNT validados por lote, não só último lote"
    - "Remessa e retorno usam parser multi-lote"
  artifacts:
    - path: src/lib/cnab/parse-240.ts
      provides: Modelo lotes[] + getPrimaryLote()
    - path: src/lib/cnab/fixtures.ts
      provides: remessa240MultiLote(), retorno240MultiLote()
  key_links:
    - from: src/lib/cnab/validate.ts
      to: src/lib/cnab/parse-240.ts
      via: validate240() iterando lotes
---

<objective>
Suportar CNAB240 multi-lote: parser com `lotes[]`, validação estrutural por lote, compat helper para código legado.

Purpose: Corrigir bug onde headerLote/trailerLote singulares sobrescrevem lotes anteriores.
Output: parse-240 refatorado, validate/validate-retorno atualizados, fixtures e testes.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-base-confi-vel/01-CONTEXT.md
@src/lib/cnab/parse-240.ts
@src/lib/cnab/validate.ts
@src/lib/cnab/validate-retorno.ts
@src/lib/cnab/fixtures.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refatorar parse240 para lotes[]</name>
  <files>src/lib/cnab/parse-240.ts</files>
  <read_first>src/lib/cnab/parse-240.ts, src/lib/cnab/positions.ts</read_first>
  <action>
    Introduzir tipo `Parsed240Lote { headerLote, details, trailerLote }`.
    `Parsed240` ganha `lotes: Parsed240Lote[]` mantendo campos legados via helper:
    - `getPrimaryLote(parsed)` → lotes[0] ou null
    - `headerLote`/`trailerLote` getters ou atribuição via primary para compat
    Loop: ao encontrar tipo 1, iniciar novo lote; tipo 3 → details do lote atual; tipo 5 → fechar lote.
    Manter headerArquivo, trailerArquivo, other, segmentos globais ou por-lote conforme necessário.
  </action>
  <verify>npm test -- parse-240 2>/dev/null || npm test</verify>
  <acceptance_criteria>
    - parse-240.ts contém `lotes` array
    - getPrimaryLote exportado
  </acceptance_criteria>
  <done>Parser agrupa múltiplos lotes sem sobrescrever</done>
</task>

<task type="auto">
  <name>Task 2: Validar contadores por lote + fixtures multi-lote</name>
  <files>src/lib/cnab/validate.ts, src/lib/cnab/validate-retorno.ts, src/lib/cnab/fixtures.ts, src/lib/cnab/parse-240.test.ts</files>
  <read_first>src/lib/cnab/validate.ts (validate240), src/lib/cnab/validate-retorno.ts</read_first>
  <action>
    Atualizar validate240 em validate.ts e equivalente retorno:
    - Iterar `parsed.lotes` validando T240_LOTE_COUNT por lote
    - Manter validação trailer arquivo global
    Adicionar fixtures `remessa240MultiLote()` e `retorno240MultiLote()` (2 lotes mínimos).
    Criar parse-240.test.ts com caso multi-lote + integração validateRemessa/validateRetorno.
  </action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - Fixture multi-lote existe
    - Teste multi-lote passa
    - validate240 não usa só último headerLote
  </acceptance_criteria>
  <done>BUG-03 coberto com testes remessa e retorno</done>
</task>

</tasks>

<verification>
- [ ] npm test
- [ ] npm run build
</verification>

<success_criteria>
- Multi-lote parseado e validado corretamente
- Compat helper preserva consumidores single-lote
</success_criteria>

<output>
After completion, create `.planning/phases/01-base-confi-vel/01-02-SUMMARY.md`
</output>
