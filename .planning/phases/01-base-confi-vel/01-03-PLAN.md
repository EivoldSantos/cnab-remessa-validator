---
phase: 01-base-confi-vel
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/lib/cnab/detect.ts
  - src/lib/cnab/detect.test.ts
  - src/lib/cnab/parse-400.ts
  - src/lib/cnab/parse-400.test.ts
  - src/lib/cnab/validate-shared.ts
  - src/lib/cnab/validate-shared.test.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [TEST-01, INFRA-01]
must_haves:
  truths:
    - "detect.ts, parse-400.ts, validate-shared.ts têm testes unitários dedicados"
    - "Push/PR em main dispara CI com test + build"
    - "npm audit roda mas não bloqueia merge"
  artifacts:
    - path: src/lib/cnab/detect.test.ts
      provides: Testes bordas 239/241/399/401
    - path: .github/workflows/ci.yml
      provides: Pipeline CI
  key_links:
    - from: .github/workflows/ci.yml
      to: package.json
      via: npm test, npm run build
---

<objective>
Fechar gaps de testes core (detect, parse-400, validate-shared) e adicionar pipeline CI GitHub Actions.

Purpose: Impedir regressões silenciosas; base de qualidade contínua.
Output: 3 arquivos de teste + workflow CI.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-base-confi-vel/01-CONTEXT.md
@src/lib/cnab/detect.ts
@src/lib/cnab/parse-400.ts
@src/lib/cnab/validate-shared.ts
@vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Testes unitários detect + parse-400 + validate-shared</name>
  <files>src/lib/cnab/detect.test.ts, src/lib/cnab/parse-400.test.ts, src/lib/cnab/validate-shared.test.ts</files>
  <read_first>src/lib/cnab/detect.ts, src/lib/cnab/parse-400.ts, src/lib/cnab/validate-shared.ts, src/lib/cnab/validate.test.ts</read_first>
  <action>
    Criar testes co-localizados, labels em português, sem mocks:
    - detect.test.ts: bordas 239/241/399/401, dominante 240 vs 400, linhas vazias
    - parse-400.test.ts: header/detail/trailer parsing, registro 0/1/9
    - validate-shared.test.ts: cobertura ampla — validateCommon, checkKindMismatch, validateBankRules, push()
    Seguir padrões de field-engine.test.ts e validate.test.ts.
  </action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - 3 novos arquivos *.test.ts existem
    - npm test passa (todos os testes)
  </acceptance_criteria>
  <done>TEST-01 satisfeito para módulos além de parse-240</done>
</task>

<task type="auto">
  <name>Task 2: GitHub Actions CI</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>package.json, vite.config.ts</read_first>
  <action>
    Criar workflow:
    - Triggers: push e pull_request em branch main
    - Node LTS (20 ou 22)
    - Steps: checkout, setup-node, npm ci, npm test, npm run build
    - Job separado ou step npm audit com continue-on-error: true
    Cache npm opcional.
  </action>
  <verify>npm test && npm run build</verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml existe
    - YAML válido com jobs test e build
  </acceptance_criteria>
  <done>INFRA-01 pipeline definido</done>
</task>

</tasks>

<verification>
- [ ] npm test
- [ ] npm run build
- [ ] Workflow YAML sintaxe válida (actionlint opcional)
</verification>

<success_criteria>
- Testes core adicionados e passando
- CI configurado conforme CONTEXT D-17/D-18
</success_criteria>

<output>
After completion, create `.planning/phases/01-base-confi-vel/01-03-SUMMARY.md`
</output>
